# 权限过滤说明

## 为什么要先过滤权限

企业 RAG 不能先把所有文件都搜一遍，再把无权限内容藏起来。
正确顺序是先过滤，再检索。

## 应该先按什么过滤

先按这两个字段过滤：

- `knowledge_base`
- `permission_group`

## 这样做的原因

- 无权限内容不会进入候选集
- 模型不会先看到再被隐藏
- 整个链路更安全、更容易审计

## 这一步和后面的关系

只有权限先过滤对了，后面的向量检索、重排和回答才有意义。

## 一句话

先控权限，再做检索。
